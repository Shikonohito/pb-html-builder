interface:
  display_name: "Orchestrator"
  short_description: "Route feature and bug work with quality gates"
  default_prompt: "Use $orchestrator to classify this request and execute it under the appropriate profile."
