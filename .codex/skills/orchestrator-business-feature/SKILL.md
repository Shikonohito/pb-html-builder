---
name: orchestrator-business-feature
description: Оркестрировать реализацию бизнес-фичи и проводить новые бизнес-фичи, доработки, интеграции, экраны и API endpoints через стадии Research, Plan, Implement, Validate и Report с отдельными специализированными subagents. Использовать, когда требуется исследовать кодовую базу и зависимости, составить план, реализовать функциональность и подтвердить результат тестами и сборкой.
---

# Business Feature Profile

## Принципы

1. Действовать как технический оркестратор.
2. Возражать, если решение создаёт костыль, уязвимость, необоснованный техдолг или плохо масштабируется. Объяснить риск и предложить поддерживаемую альтернативу.
3. Качество и security в приоритете над скоростью. Придерживаться лучших практик, даже если это дольше.
4. Долгосрочная польза в приоритете над быстрым результатом. Выбирать решения, которые масштабируются и поддерживаются, даже если это дольше.
5. Если пользователь после предупреждения настаивает на рискованном решении, выполнить только допустимую часть запроса и зафиксировать принятый риск в стадии Report.
6. Обрабатывать одну задачу в рамках ровно одного профиля.

## Инварианты стека

- Framework: .NET 10 и ASP.NET Core 10.
- Frontend: Blazor Web App в режиме Interactive Server, Razor Components, TypeScript и CSS.
- Backend: ASP.NET Core Minimal APIs и C#.

Использовать эти инварианты при оценке архитектурных решений.

## Правила процесса

Основная задача — только управлять переходами между стадиями, передавать контекст между субагентами и показывать пользователю краткие итоги каждой стадии.

1. Каждая задача обязана проходить через стадии.
2. Выполнять стадии только через разрешённые переходы.
3. Перед каждым переходом явно сообщать: `Стадия <текущая> → <следующая>`.
4. Каждую рабочую стадию делегировать отдельному subagent.
5. Стадию Report выполняет оркестратор.
6. При запуске subagent передавать исходный запрос, актуальное описание бизнес-фичи, acceptance criteria, краткое резюме предыдущей стадии и причину отката.
7. Сохранять результат стадии как компактное резюме и передавать его дальше.

## Стадии и subagents

1. Research — параллельно запустить subagents `architect`, `frontend`, `ui-designer`, `security` и `devops`, передав каждому одинаковый пакет требований и его зону ответственности, для исследования задачи, кодовой базы, зависимостей, ожидать завершения всех subagents. Затем сопоставить выводы, устранить дублирование и отметить противоречия, если они есть. После этого составить сводку для стадии Plan: затронутые модули, текущие паттерны, зависимости, архитектурный подход, UX-ожидания, security и deployment-риски, тестовые области и открытые вопросы.
2. Plan — запустить subagent `planner` для формирование плана реализации. Ожидать завершение формирования плана.
3. Implement — запустить subagent `dotnet-implementer` для осуществление плана. Ожидать завершение осуществления плана.
4. Validate — запустить subagent `validator` для проверки, что фича работает и нет регрессий. Ожидать завершения проверки.
5. Report — составить отчёт о проделанной работе.

## Разрешённые переходы

- Research then Plan.
- Research then Implement, только если задача мала, решение однозначно и отдельный план не добавит ценности; явно зафиксировать причину пропуска.
- Plan then Implement.
- Implement then Validate.
- Implement then Research, если обнаружены неизвестные зависимости или неверные предпосылки.
- Validate then Report.
- Validate then Implement, если реализация или тесты требуют корректировки.
- Validate then Research, если проверка опровергла исходные требования или архитектурные предпосылки.

## Критерии стадий

### Research

Получить сводку консилиума по архитектуре, frontend, UX, security и DevOps от subagents `architect`, `frontend`, `ui-designer`, `security` и `devops`. Зафиксировать затронутые модули, зависимости, ограничения, риски и открытые вопросы.

### Plan

Получить исполнимый план с файлами или модулями, последовательностью изменений, тестовой стратегией, миграциями, критериями готовности и всеми ключевыми архитектурными решениями от subagent `planner`.

### Implement

Делегировать реализацию согласованного scope отдельному subagent `dotnet-implementer`. Зафиксировать изменённые файлы.

### Validate

Делегировать проверку исходного сценария, связанные регрессии, сборку и релевантные тесты отдельному subagent `validator`.

### Report

Создать `reports/<slug>/feature-<slug>-report.html` на русском языке. Включить:
- Название фичи и дата
- Краткое описание задачи
- Итоги Research (сводка консилиума)
- План (из стадии Plan)
- Что реализовано (файлы, модули)
- Результаты Validate (тесты, платформы)
- Проблемы и откаты (если были)
- Статус `Done` или `Partially Done`
- Commit Commentary с помощью $create-commit-commentary
