@page "/builder/practice"

<BuilderWorkspaceShell DocumentKind="Practice"
                       DocumentTitle="@ResolvedTitle"
                       Accent="#b892ff"
                       OutlineLabel="Оглавление"
                       MapTitle="Task navigation"
                       ShowSectionMap="false"
                       PrimarySectionTitle="Task prompt"
                       SidebarFooter="© Ivan Ivanov, 2026"
                       OutlineItems="OutlineItems"
                       CanvasItems="CanvasItems" />

@code {
    private static readonly string[] OutlineItems =
    [
        "Task 01"
    ];

    private static readonly string[] CanvasItems =
    [
        "Prompt",
        "Hint",
        "Code",
        "Run",
        "Debug",
        "Tests"
    ];

    [SupplyParameterFromQuery(Name = "title")]
    public string? Title { get; set; }

    [SupplyParameterFromQuery(Name = "folderPath")]
    public string? FolderPath { get; set; }

    [SupplyParameterFromQuery(Name = "fileName")]
    public string? FileName { get; set; }

    private string ResolvedTitle => string.IsNullOrWhiteSpace(Title)
        ? "Название практики"
        : Title.Trim();
}
